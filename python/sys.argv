import sys
import os


    if len(sys.argv) < 4:
        print('*** Usage:', file=sys.stderr)
        print(f'    {sys.argv[0]}   interval   file1   file2   [file3   [...]]', file=sys.stderr)
        print(os.linesep, file=sys.stderr)
        sys.exit(1)

    ARG1 = sys.argv[1]
